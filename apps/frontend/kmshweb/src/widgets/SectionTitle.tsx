const SectionTitle = ({ title }: { title: string }) => {
  return (
    <p className="text-xs font-semibold tracking-wide uppercase opacity-40 ms-4 mt-4 mb-1">
      {title}
    </p>
  );
};

export default SectionTitle;
