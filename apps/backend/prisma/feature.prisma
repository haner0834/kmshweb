model Feature {
    id          Int      @id @default(autoincrement())
    title       String
    code        String   @unique
    description String?
    isUpcoming  Boolean  @default(true)
    ratings     Rating[]
    reports     Report[]
}
